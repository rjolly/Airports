# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.AirportController.index

# Widgets
GET     /widgets                    controllers.WidgetController.listWidgets
POST    /widgets                    controllers.WidgetController.createWidget

# Airports
GET     /airports                   controllers.AirportController.listAirports

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
